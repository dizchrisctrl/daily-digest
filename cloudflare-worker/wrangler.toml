name            = "daily-digest-proxy"
main            = "index.js"
compatibility_date = "2024-09-23"

# After deploying, set your Anthropic key as a secret (not in this file):
#   wrangler secret put ANTHROPIC_API_KEY
#
# Then copy the deployed URL (e.g. https://daily-digest-proxy.<you>.workers.dev)
# into your GitHub repo as a secret named WORKER_URL.
